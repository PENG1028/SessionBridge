name: session_bridge
description: "SessionBridge multi-platform client — embedded Node.js relay + WebView dashboard."
publish_to: 'none'
version: 0.2.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.10.0
  webview_flutter_wkwebview: ^3.16.0
  flutter_local_notifications: ^18.0.1
  shared_preferences: ^2.3.4
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
