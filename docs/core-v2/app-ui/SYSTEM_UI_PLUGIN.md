# Historical Redirect: System UI Plugin

This file name is retained only so old links do not break.

The current concept is **App UI**, not System UI. Use
[APP_UI_PLUGIN.md](APP_UI_PLUGIN.md) as the source of truth.

Rules for future work:

- Do not add new references to `System UI` except when explaining historical
  migration.
- Do not use this file as an implementation guide.
- Do not reintroduce `system-ui` as a product name. The only acceptable
  remaining use is the wire-compatible manifest adapter key
  `adapters.system-ui`.
